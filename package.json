{
  "name": "class-clash-root",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:shared": "npm run build --prefix packages/shared",
    "dev:server": "npm run dev --prefix apps/server",
    "dev:client": "npm run dev --prefix apps/client",
    "dev": "concurrently \"npm run dev --prefix apps/server\" \"npm run dev --prefix apps/client\"",
    "build": "npm run build --prefix packages/shared && npm run build --prefix apps/client && npm run build --prefix apps/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
